// Public entry for @eidra/tokens.
// `./_generated` is produced by build.mjs (Style Dictionary) before tsup runs.
// Each export is a CSS variable reference string, e.g. `colorAccent === "var(--eidra-accent)"`,
// so values stay theme-aware at runtime.
export * from './_generated.js';
