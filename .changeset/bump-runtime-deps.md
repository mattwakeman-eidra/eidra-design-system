---
"@eidra/react": patch
"@eidra/icons": patch
---

Bump shipped runtime dependencies: Base UI to 1.6 (`@eidra/react`), and lucide-react to 1.21 + country-flag-icons to 1.6.18 (`@eidra/icons`). Also updates the `style-dictionary` build tool to 5.5 (no change to the generated token output).
