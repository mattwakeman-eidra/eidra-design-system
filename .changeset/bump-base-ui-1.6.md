---
'@eidra/react': patch
---

Bump Base UI to 1.6 (`@eidra/react` runtime dependency). 1.6 removes arrow-key roving focus from Accordion to match the updated APG guidance (headers are now plain buttons in the Tab order; Enter/Space still toggles); the Accordion keyboard story is updated to reflect this.
