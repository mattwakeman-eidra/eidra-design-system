---
---

Docs: add an "Inline term (glossary)" story to `Tooltip`, demonstrating the dashed-underline term-with-definition pattern (replacing the need for a separate `Term` component). No API change.
