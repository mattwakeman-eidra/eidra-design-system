---
'@eidra/react': patch
---

`ScrollArea`: the thumb now reacts to the Base UI 1.6 `data-scrolling` hook, deepening its colour while the user is actively scrolling (previously only the scrollbar track responded).
