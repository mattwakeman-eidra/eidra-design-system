---
'@eidra/react': patch
---

Compact density now reduces horizontal padding/gap (not just height) on Button, Input, NumberField, Toggle, PeriodNavigator, Collapsible, FilterSelect, and the DataGrid editable cells. Fix the ScrollArea scrollbar thumb overflowing its track, and add a horizontal-orientation gap variant to RadioGroup.
